// Stub — replace with actual decorative component when available
export function BarbedWireLine({ className, ...rest }: { className?: string; intensity?: string; [key: string]: unknown }) {
  return <hr className={className} aria-hidden="true" />
}
