// Stub — replace with actual SVG logo when available
export function HireWireLogo({ className, ...rest }: { className?: string; variant?: string; size?: string; [key: string]: unknown }) {
  return (
    <span className={className} aria-label="HireWire">
      HireWire
    </span>
  )
}
